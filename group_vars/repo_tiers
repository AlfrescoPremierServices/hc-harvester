# Manually define alfresco related variables if autodetect fails.
# All variables defined bellow can be defined per server using ansible
# host_vars or through subset of groups (like share_tiers or repo_tiers)

# uncomment if your tomcat instance has a shared loader location wich doesn't
# match ${CATALINA_BASE}/shared/classes
#alfresco_shared_loader: /opt/alfresco/extension-root

# Set custom application log pattern (use wildcadrs as needed)
#alfresco_log_pattern: /var/log/alfresco/alfresco.log*
