---
# temp location where everything will be archived before fetching
hc_tmp: /tmp/hc

# Set the log history to archive and fetch (default 1 week)
log_history: "-1w"

# Use authentication wrapper to handle `sudo su` only configs
ansible_become_exe: "{{ hc_tmp }}/bin/sudosu-wrapper.sh"
